# Diagrams

Exported architecture diagrams live here as image files (`.png` / `.svg`).

If a diagram has an editable source (draw.io, Excalidraw, Figma, etc.), commit the source file here too, next to its exported image, using the same base filename (e.g. `system-architecture.drawio` + `system-architecture.png`).

Nothing has been added yet — see [../system-overview.md](../system-overview.md) for the list of diagrams to create.
