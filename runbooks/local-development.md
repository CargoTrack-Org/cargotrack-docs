# Local Development

> Status: stub.

## Prerequisites

_TODO: list required tools (Docker, Node version, AWS CLI, etc.)_

## Running the stack

`cargotrack-app` ships a `docker-compose.yml` for local development. Document here:

- How to configure `.env` (based on `.env.example`)
- Which command starts the stack (`docker-compose up`, etc.)
- How to seed test data / create a first admin user
- Known gotchas for local setup

## Running a single service

_TODO: how to run one service (e.g. `ai-service`) outside Docker for faster iteration._
